NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://resolvex_dev:resolvex_dev_password@localhost:5432/resolvex_dev?schema=public
JWT_SECRET=replace_with_real_local_development_secret
JWT_EXPIRES_IN=1h
DOCUMENT_STORAGE_PROVIDER=local
LOCAL_DOCUMENT_STORAGE_PATH=./storage/documents
AWS_REGION=us-east-1
AWS_S3_BUCKET=
AI_SERVICE_BASE_URL=http://localhost:8000

# Demo seed passwords.
# These are for local development prototypes only. Change them outside local development.
# If omitted, the seed script uses the documented development-only fallback: ResolveXDemo123!
RESOLVEX_DEMO_PASSWORD=
RESOLVEX_DEMO_MEMBER_PASSWORD=
RESOLVEX_DEMO_MERCHANT_PASSWORD=
RESOLVEX_DEMO_ANALYST_PASSWORD=
